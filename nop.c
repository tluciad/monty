#include "monty.h"
/**
 * nop - doesn’t do anything
 *
 * @s: name of the stack
 * @line_number: new item to push
 * Return: Always 0
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)s;
	(void)line_number;
}
